# example of foc.bat required to run and save measurements
    #cd c:\labhub\import
    #start /WAIT FOC48_20230523.exe myrPlate.xml -e %1


# bat file required to open gui from terminal

@echo off
cd /d "C:\Users\Acer\PycharmProjects\open-biohit-plus"
call .venv\Scripts\activate.bat
pipettor-gui
pause

#if mac, .sh file
#!/bin/bash
# Mac/Linux launcher for Biohit Pipettor Plus GUI

# Navigate to project directory
cd "/Users/advaitlath/PycharmProjects/open-biohit-plus" || {
    echo "Error: Could not find project directory"
    echo "Please update the path in this script to match your installation"
    exit 1
}

# Run the GUI using virtual environment Python
source .venv/bin/activate
pipettor-gui

# Wait for user input before closing (optional)
read -p "Press Enter to close..."
