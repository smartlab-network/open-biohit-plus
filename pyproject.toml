[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "open-biohit-plus"
version = "0.0.1"
description = "Extention for open-biohit"
authors = ["Thiago Meyer <>"]
license = "MIT"
readme = "README.md"
packages = [{ include = "src/biohit_pipettor_plus" }]
repository = "https://github.com/deinuser/mein_projekt"

[tool.poetry.dependencies]
python = "^3.9"
open-biohit = "^0.0.2"

